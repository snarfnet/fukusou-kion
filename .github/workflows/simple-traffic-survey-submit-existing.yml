name: SimpleTrafficSurvey Submit Existing Build

on:
  workflow_dispatch:
    inputs:
      buildNumber:
        description: "Uploaded App Store Connect build number"
        required: true
        default: "103"
      priceJpy:
        description: "Japan customer price in JPY"
        required: true
        default: "300"
  push:
    branches:
      - codex/simple-traffic-survey-production
    paths:
      - ".github/workflows/simple-traffic-survey-submit-existing.yml"
      - "ios/SimpleTrafficSurvey/scripts/submit_production.py"

concurrency:
  group: simple-traffic-survey-submit-existing
  cancel-in-progress: true

jobs:
  submit-existing:
    runs-on: macos-latest
    defaults:
      run:
        working-directory: ios/SimpleTrafficSurvey
    env:
      APP_BUNDLE_ID: com.tokyonasu.simpletrafficsurvey
      APP_NAME: "\u7C21\u6613\u6B69\u884C\u8005\u4EA4\u901A\u91CF\u8ABF\u67FB"
      APP_SKU: simple-traffic-survey-ios
      BUNDLE_NAME: SimpleTrafficSurvey
      APP_VERSION: "1.0"
      BUILD_NUMBER: ${{ inputs.buildNumber || '103' }}
      APP_PRICE_JPY: ${{ inputs.priceJpy || '300' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Python dependencies
        run: python3 -m pip install --break-system-packages --upgrade PyJWT cryptography requests

      - name: Write App Store Connect key
        env:
          ASC_API_KEY_CONTENT: ${{ secrets.ASC_API_KEY_CONTENT }}
          ASC_PRIVATE_KEY: ${{ secrets.ASC_PRIVATE_KEY }}
          ASC_KEY_ID_SECRET: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID_SECRET: ${{ secrets.ASC_ISSUER_ID }}
        run: |
          test -n "$ASC_KEY_ID_SECRET"
          test -n "$ASC_ISSUER_ID_SECRET"
          echo "ASC_KEY_ID=$ASC_KEY_ID_SECRET" >> "$GITHUB_ENV"
          echo "ASC_ISSUER_ID=$ASC_ISSUER_ID_SECRET" >> "$GITHUB_ENV"
          mkdir -p /tmp/asc
          if [ -n "$ASC_PRIVATE_KEY" ]; then
            printf '%s' "$ASC_PRIVATE_KEY" > "/tmp/asc/AuthKey_${ASC_KEY_ID_SECRET}.p8"
          else
            test -n "$ASC_API_KEY_CONTENT"
            printf '%s' "$ASC_API_KEY_CONTENT" | base64 --decode > "/tmp/asc/AuthKey_${ASC_KEY_ID_SECRET}.p8"
          fi
          chmod 600 "/tmp/asc/AuthKey_${ASC_KEY_ID_SECRET}.p8"
          cp "/tmp/asc/AuthKey_${ASC_KEY_ID_SECRET}.p8" /tmp/asc_key.p8

      - name: Resolve App Store Connect app
        env:
          ASC_P8_PATH: /tmp/asc_key.p8
          BUNDLE_ID: ${{ env.APP_BUNDLE_ID }}
        run: |
          if [ -n "${{ secrets.SIMPLE_TRAFFIC_SURVEY_APP_ID }}" ]; then
            echo "APP_ID=${{ secrets.SIMPLE_TRAFFIC_SURVEY_APP_ID }}" >> "$GITHUB_ENV"
          else
            python3 scripts/ensure_asc_app.py | tee /tmp/simple-traffic-survey-ensure-app.log
            APP_ID_VALUE="$(grep '^APP_ID=' /tmp/simple-traffic-survey-ensure-app.log | tail -1 | cut -d= -f2)"
            test -n "$APP_ID_VALUE"
            echo "APP_ID=$APP_ID_VALUE" >> "$GITHUB_ENV"
          fi

      - name: Set JPY price and submit uploaded build
        env:
          ASC_P8_PATH: /tmp/asc_key.p8
        run: python3 scripts/submit_production.py
