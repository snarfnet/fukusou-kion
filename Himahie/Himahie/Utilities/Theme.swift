import SwiftUI
import UIKit

enum Theme {
    static let blue = Color(red: 0.10, green: 0.45, blue: 0.85)
    static let deepBlue = Color(red: 0.02, green: 0.18, blue: 0.40)
    static let cyan = Color(red: 0.12, green: 0.65, blue: 0.92)
    static let ice = Color(red: 0.88, green: 0.96, blue: 1.0)
    static let surface = Color(uiColor: .secondarySystemBackground)
    static let elevatedSurface = Color(uiColor: .systemBackground)
    static let divider = Color(uiColor: .separator).opacity(0.45)
}
