# Lost in Japan

iOS 17以降向けのSwiftUIアプリです。案件はSwiftDataへ保存し、写真を含む個人情報を外部へ送りません。

## 開き方

1. macOSでXcodeGenをインストールします。
2. このフォルダで `xcodegen generate` を実行します。
3. `LostInJapan.xcodeproj` をXcodeで開きます。

Phase 1では、オンボーディング、紛失登録、回収プラン、日本語カード、音声読み上げ、案件一覧・詳細、交番検索、設定を実装しています。

