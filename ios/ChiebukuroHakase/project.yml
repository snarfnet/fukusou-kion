name: ChiebukuroHakase
options:
  bundleIdPrefix: com.tokyonasu
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.4"

settings:
  base:
    SWIFT_VERSION: "5.9"
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    TARGETED_DEVICE_FAMILY: "1,2"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: 10
    DEVELOPMENT_TEAM: 83VGKGSQUH

targets:
  ChiebukuroHakase:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: ChiebukuroHakase
    settings:
      base:
        PRODUCT_NAME: ChiebukuroHakase
        PRODUCT_BUNDLE_IDENTIFIER: com.tokyonasu.chiebukurohakase
        INFOPLIST_FILE: ChiebukuroHakase/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    scheme: {}
