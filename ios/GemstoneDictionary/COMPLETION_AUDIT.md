# 完成監査

## 目的

天然石の辞書アプリとして、カメラ判定、辞典検索、五十音一覧、詳細データ、市場価格目安、翡翠データ、サイズ表示を備える。

## 現在満たしている項目

- iOSアプリとして `GemstoneDictionary.xcodeproj` を同梱
- SwiftUI画面あり
- ライブカメラ判定あり
- 翡翠とターコイズのサンプル判定あり。実機カメラなしでも結果画面を確認できる
- ライブカメラ中に確認中/安定表示を出す
- 暗い、明るすぎる、色が弱い、石が小さいときの撮影アドバイスを表示
- カメラ画面に石と基準物のフレーミングガイドを表示
- 撮影判定あり
- 写真ライブラリからの判定あり
- 候補欄に判定確度を表示し、高め/中くらい/低めで撮り直しや鑑別書確認を案内
- 候補カードから任意の候補を詳細表示し、履歴保存できる
- 最近の判定履歴を表示し、候補、レベル、サイズ、基準物、相場を見返せる
- 判定履歴は画像を保存せず、石ID、スコア、レベル、サイズ、基準物、時刻を端末内に保存する
- 候補カードと履歴カードから判定結果をテキスト共有でき、サイズ推定に使った基準物も残る
- 辞典詳細から天然石詳細メモを共有でき、硬度、比重、屈折率、相場、処理、鑑別書注意を含む
- 候補名、一致率、ランク候補、透明度、サイズ、市場価格を表示
- 辞典詳細にS/A/B/Cの「ランク目安」を表示
- 辞典詳細に「数値の見方」を表示し、硬度、比重、屈折率の意味を説明
- 辞典詳細に「処理・注意ハイライト」を表示し、染色、含浸、樹脂、再生品、扱い方を確認できる
- サイズは基準物なしの場合「見かけサイズ」と表示
- 基準物ありの場合「約mm目安」と表示
- サイズ推定の信頼度を高め/中くらい/低めで表示し、撮り直し目安を出す
- 辞典タブあり
- 五十音チップあり
- 五十音セクション見出しあり
- 色フィルターで辞典を絞り込める
- ランク目安フィルターで辞典を絞り込める
- 辞典の該当なし表示と条件リセットを表示
- お気に入りを端末内に保存し、辞典で絞り込める
- 検索あり
- `た` でターコイズが出る
- `ヒスイ`、`jade` で翡翠が出る
- 150種類以上の天然石データあり
- 翡翠データあり
- ターコイズデータあり
- 各石に硬度、比重、屈折率、透明度、処理、見分け方、価格条件、扱い方あり
- 市場価格に `2026年6月目安` を表示
- 写真判定の仕組みと限界をアプリ内に表示
- 写真判定後に色相、彩度、明るさ、画面内サイズを使った「判定の根拠」を表示
- 写真のレベル表示と鑑定ランクの違いを詳細画面で説明
- 辞典詳細に「似ている石」を表示し、色相差と彩度差で比較できる
- 辞典詳細に「迷いやすい石」を表示し、翡翠/ネフライト、ターコイズ/アマゾナイトなどを硬度、比重、処理で比較できる
- 辞典詳細に「購入前チェック」を表示し、相場、処理、鑑別書、返品条件を確認できる
- 辞典詳細に「鑑別・ランク確認メモ」を表示し、名称、硬度、比重、屈折率、処理、価格目安を照合できる
- 相場タブに「150種類以上の相場一覧」を表示し、石名、ランク目安、市場価格を一覧で確認できる
- 相場タブに「相場更新で見る条件」を表示し、価格更新時の確認項目を説明
- 相場タブに「買う前に見る項目」を表示し、処理、証明、品質、価格を購入前に確認できる
- 相場タブに「鑑別・相場の用語集」を表示し、A貨翡翠、B貨/C貨、含浸、染色、安定化、再生品、硬度、比重、屈折率、カラット、鑑別書を確認できる
- `MARKET_DATA_NOTES.md` に相場更新の出典と運用ルールを整理
- App Store説明文、プライバシーポリシー、TestFlightメモ、スクリーンショット案あり
- 静的検証スクリプトあり
- GitHub ActionsのiOSビルドCIあり
- GitHub ActionsのUIテストCIあり
- GitHub Actionsのシミュレータ用artifactあり
- iPhone実機で完成判定するための `REAL_DEVICE_QA.md` あり
- iPhone実機ビルドとTestFlight手順の `DEVICE_BUILD_AND_TESTFLIGHT.md` あり

## 検証済み

`python scripts/verify_static.py` で以下を確認済み。

- 辞書データ件数
- 各石の詳細フィールド
- 翡翠、ターコイズ
- 検索の表記揺れ
- 市場価格、相場レビュー日、注意文
- 透明度、ランク、サイズ
- ライブカメラ、撮影、写真選択の実装参照
- サンプル判定の実装参照
- Xcodeプロジェクト、共有スキーム、Info.plist
- AppIcon、PrivacyInfo、JSON、plist
- GitHub Actionsワークフロー
- GitHub Actions `GemstoneDictionary iOS Build` の成功
- UIテストターゲットとスキーム設定
- シミュレータ用artifactのアップロード設定
- 実機QAシート

## 未検証

このWindows環境では確認できないため、完了前にMacまたはiPhone実機で確認する。

- `bash scripts/build_mac.sh` の成功
- iPhone実機でライブカメラが起動する
- iPhone実機でカメラ許可拒否時の表示が崩れない
- iPhone実機で撮影後に候補、サイズ、透明度、価格が表示される
- iPhone実機で写真ライブラリ選択が動く
- 小さい画面でテキストが重ならない

## 完了判断

静的な成果物とGitHub ActionsのXcodeビルドは要件を満たしている。アプリとして完成扱いにするには、`REAL_DEVICE_QA.md` に沿ったiPhone実機カメラ確認が必要。
