# KAZU

標準・関数・金融・換算・日付・統計・図形を1本にまとめたiOS電卓です。

## 開き方

1. MacにXcodeGenを入れます: `brew install xcodegen`
2. このフォルダで `xcodegen generate`
3. `KAZU.xcodeproj` をXcodeで開き、Signing Teamを選んで実行します

iOS 17以降を対象にしています。

`KAZU/DesignReference/kazu-imagegen-reference.png` はimagegenで作ったデザイン原案です。

